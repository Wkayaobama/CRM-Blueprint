



Outputs (vault)
For each record:

Generated record note:
crm/<Entity>/<Team>/<slug(name or id)>.md
Companion note (user-owned):
crm/<Entity>/<Team>/<slug(name or id)>.notes.md (created if missing, never overwritten)
Also:

View notes from YAML at:
crm/<Entity>/_teams/<Team>/views/<View Name>.md
(foundation scaffolding now; later we’ll materialize static tables + optional Dataview)
And:

Mapping report:
crm/_system/MappingReport.md